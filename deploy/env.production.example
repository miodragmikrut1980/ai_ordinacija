# Copy to /opt/clinic-ai-assistant/.env.production and chmod 600.
# CLINIC_ENV=production (set in the unit file) makes the app refuse to start
# without this being set explicitly -- see backend/app/state.py.
CLINIC_ENCRYPTION_KEY=REPLACE_WITH_A_KEY_FROM_YOUR_SECRETS_MANAGER
